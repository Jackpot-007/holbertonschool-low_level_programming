#include <stdlib.h>
#include "main.h"

/**
 * _abs - this calculates the absolute value of an integer.
 *
 * @x: wawawawawa
 *
 * Return: Always 0 (success)
 */

int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
